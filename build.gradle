import com.manning.gia.sanitycheck.tasks.BuildExerciser

ext.supportedGradleVersion = '5.5'

task verifyListings(type: BuildExerciser,
        group: 'Gradle in Action',
        description: 'Verifies that listings can be run successfully.') {
    gradleVersion = supportedGradleVersion
}
